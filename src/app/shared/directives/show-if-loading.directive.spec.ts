import { ShowIfLoadingDirective } from './show-if-loading.directive';

describe('ShowIfLoadingDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowIfLoadingDirective();
    expect(directive).toBeTruthy();
  });
});
