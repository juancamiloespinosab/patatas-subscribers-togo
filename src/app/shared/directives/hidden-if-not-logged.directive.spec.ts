import { HiddenIfNotLoggedDirective } from "./hidden-if-not-logged.directive";

describe('HiddenIfNotLoggedDirective', () => {
  it('should create an instance', () => {
    // const directive = new HiddenIfNotLoggedDirective();
    // expect(directive).toBeTruthy();
  });
});
